version: '3.4'
services:
  ui:
    build:
      context: ./
      target: dev
    volumes:
      - .:/app
    command: yarn run dev
    ports:
      - "3070:3070"
    environment:
      NODE_ENV: development
      DEBUG: nodejs-docker-express:*
    restart: always
